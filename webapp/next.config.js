/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment the following line if you deploy to a subpath
  // basePath: '',
};

module.exports = nextConfig;
